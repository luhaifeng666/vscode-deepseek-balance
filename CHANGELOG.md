# 更新日志

本文件记录本扩展的所有重要变更。

## [0.2.0] - 2026-09-14

悬停提示里新增用量数据。

- 悬停提示中显示金额、请求次数与输入/输出 token（含缓存命中 / 未命中）
- 支持切换时间范围：今日 / 近 7 天 / 本月。今日用本地日界；近 7 天与本月按 UTC 日
  切分（接口只接受 UTC 零点对齐的长窗口），悬停里会明确标出窗口起止
- 用量需要第二枚凭据（控制台的会话 token），与新命令「设置 / 清除用量 Token」。
  它同样只存在于系统钥匙串，不写入设置、不参与 Settings Sync
- **用量出任何问题都不影响余额**：Token 过期、接口改结构、网络失败都只在悬停的
  用量段里如实说明，余额数字照旧。这条有可执行约束保证（10 个余额状态 × 5 种用量
  视图逐字节比对渲染结果）
- 没配 API Key 时**完全不查用量**——那时悬停里没有用量段，没有理由请求第三方主机。
  `refreshInterval` 为 `0` 时用量也一并停止后台轮询
- 用量接口地址**不是配置项**：请求头上挂着控制台会话凭据，一个能指向任意主机的
  开关就是一条把凭据送出去的通道。它只从环境变量读，且只接受回环地址
- 「刷新余额」命令同时刷新用量；悬停里用量出错时的「重试」链接因此真的有效
- 时间范围写入**全局**设置。若被工作区层设置覆盖，会明确提示而不是静默失败
- 扩展脱敏规则扩展：原规则只认 `sk-` 前缀，控制台 token 不是那个形状

## [0.1.1] - 2026-09-14

打包内容修正，功能无变化。

- 发布物不再夹带开发文件（内部发布手册与 CI 配置），体积由 28.87 KB 降至 15.41 KB
- 新增冒烟测试：发布前在 CI 里加载打包好的 `.vsix` 跑一遍 `activate()`，
  并核对包内不含 `src/`、`docs/` 等开发文件 —— 上面那个问题就是它抓出来的

## [0.1.0] - 2026-09-14

首个版本。

- 状态栏实时显示 DeepSeek 账户余额，支持多币种
- API Key 保存在系统钥匙串（SecretStorage），不写入设置、不参与 Settings Sync
- 启动时自动查询一次，之后按可配置间隔自动刷新，也可随时手动刷新
- 低余额阈值告警
- 点击状态栏查看详情，可切换币种、前往充值
- 网络抖动时保留最后已知良好值并标注已过期，而不是把数字清空
- 区分「密钥无效」「连接失败」「数据异常」「余额不可用于 API 调用」等状态
- 无遥测、无数据上报；除配置的接口地址外不发起任何外部请求
